'use client';

import { MobileMenuDemo } from '@/components/dashboard/MobileMenuDemo';

/**
 * Demo Page for Mobile Menu Backdrop Styles
 * 
 * This page allows testing different backdrop styles for the mobile navigation menu.
 * Access it at /demo/mobile-menu to test the different visual options.
 */
export default function MobileMenuDemoPage() {
    return <MobileMenuDemo />;
}
